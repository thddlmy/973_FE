import React from 'react';
import { PostSearchSectionContainer } from '@containers/Section';
import { LayoutColumn } from '@components/Layout';

const SearchPage = () => {
  return (
    <LayoutColumn>
      <PostSearchSectionContainer />
    </LayoutColumn>
  );
};

export default SearchPage;
